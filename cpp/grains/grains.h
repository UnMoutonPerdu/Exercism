#if !defined(GRAINS_H)
#define GRAINS_H

#include <cmath>
#include <stdexcept>

namespace grains {
    long long unsigned int square(int box);
    long long unsigned int total();
}  // namespace grains

#endif // GRAINS_H